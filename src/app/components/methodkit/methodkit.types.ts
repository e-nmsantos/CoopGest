export interface MKCard {
  id: string;
  title: string;
  titlePt: string;
  description: string;
  category: string;
  icon: string;
}

export interface CategoryMeta {
  id: string;
  label: string;
  color: string;
}
